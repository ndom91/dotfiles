#!/usr/bin/env bash
cliphist list | \
  rofi \
  -p '' \
  -window-title 'Cliphist' \
  -i \
  -format %s \
	-dmenu \
	-scroll-method 0 \
  -no-fixed-num-lines \
	-theme "$HOME"/.config/rofi/config/launcher.rasi | \
  cliphist decode | \
  wl-copy
